# Finance CRM

**Finance CRM** - це внутрішня CRM-система для страхової компанії, розроблена як навчальний проєкт.  
Система орієнтована не на клієнтський кабінет, а на **внутрішню роботу співробітників**: менеджерів, керівника відділу та адміністратора.

Основний інтерфейс реалізований у вигляді адміністративної панелі на **Filament**.

---

## Призначення проєкту

Проєкт моделює внутрішню роботу страхової компанії та покриває базові CRM-процеси:

- ведення клієнтської бази;
- оформлення та супровід страхових полісів;
- облік оплат за полісами;
- реєстрація страхових випадків;
- обробка вхідних заявок;
- розмежування доступу за ролями;
- базовий контроль активності та операцій у системі.

Система не містить повноцінної клієнтської публічної частини, але вже має підготовлену внутрішню модель для майбутнього лендингу з формою заявки.

---

## Основна ідея системи

У системі передбачено три основні ролі:

- **admin** - технічне адміністрування системи: керування користувачами, довідниками, службовими налаштуваннями та проблемними записами;
- **supervisor** - керівник відділу, який контролює роботу менеджерів і переглядає управлінську аналітику;
- **manager** - менеджер, який працює з власними клієнтами, заявками, полісами, оплатами та страховими випадками.

Логіка доступу побудована навколо ролей:

- менеджер працює лише зі **своїми клієнтами**;
- менеджер бачить лише **свої поліси**, оплати, вхідні заявки та пов’язані страхові випадки;
- менеджер може працювати з нотатками лише по страхових випадках, пов’язаних із його полісами;
- supervisor бачить загальну картину по всіх менеджерах і має доступ до управлінської аналітики;
- admin відповідає за технічне адміністрування системи, користувачів, довідники та контроль проблемних записів;
- страхові довідники доступні для перегляду всім ролям, але **редагування, створення та видалення** доступні лише **admin** та **supervisor**.

---

## Основні модулі

### 1. Клієнти

Система підтримує два типи клієнтів:

- **фізична особа**
- **компанія**

Для клієнтів зберігаються:

- тип клієнта;
- статус;
- ПІБ або назва компанії;
- контактні дані;
- номер документа;
- ІПН / ЄДРПОУ;
- дата народження (для фізичної особи);
- джерело звернення;
- закріплений менеджер;
- нотатки.

### 2. Вхідні заявки

Окремий модуль для роботи з новими зверненнями:

- фіксація заявки;
- призначення менеджера;
- зміна статусу заявки;
- конвертація заявки в клієнта.

Цей модуль можна використати як основу для майбутньої публічної форми заявки з лендингу.

### 3. Поліси

Для кожного полісу зберігаються:

- номер полісу;
- клієнт;
- страхова пропозиція;
- менеджер;
- дата початку дії;
- дата завершення дії;
- сума оплати;
- сума покриття;
- строк оплати;
- статус полісу;
- комісія;
- службові нотатки.

### 4. Оплати полісів

Для оплат ведеться облік:

- методу оплати;
- статусу оплати;
- строку оплати;
- суми;
- транзакції;
- службових дат оплати / ініціації.

Статус полісу обчислюється централізовано на основі правил бізнес-логіки та стану оплат.

### 5. Страхові випадки

Модуль страхових випадків дозволяє:

- створювати заяву по активному полісу;
- фіксувати дату події;
- вказувати місце, причину та опис;
- вносити заявлену, резервну та виплачену суму;
- вести нотатки по заяві;
- відстежувати статус кейсу.

### 6. Журнал дій користувачів

У системі реалізовано журнал дій користувачів, який дозволяє:

- логувати створення, оновлення та видалення записів;
- бачити історію змін;
- обмежувати видимість логів відповідно до ролі користувача.

> **Важливо:** журнал дій фіксує саме ручні дії користувачів у CRM: створення, редагування та видалення записів через адміністративну панель.
> Автоматичні службові операції, наприклад щоденний перерахунок статусів оплат і полісів командою `policies:daily-sweep`, не вважаються діями співробітників і не записуються в цей журнал.
> Такий підхід дозволяє відокремити активність користувачів від системної demo-логіки.

### 7. Страхові довідники

Окремий блок довідників:

- категорії страхувань;
- страхові компанії;
- страхові продукти;
- страхові пропозиції.

Для **manager** довідники доступні лише в режимі перегляду.  
Для **admin** та **supervisor** доступні створення, редагування та видалення.

---

## Дашборд

У системі реалізовано рольовий дашборд.

### Для manager:
- кількість власних клієнтів;
- кількість власних полісів;
- кількість власних страхових випадків;
- прострочені оплати по власних полісах;
- динаміка клієнтів і полісів;
- поліси, строк дії яких скоро завершується;
- нові вхідні заявки та нотатки по власних страхових випадках.

### Для supervisor:
- загальні показники по роботі менеджерів;
- динаміка клієнтів і полісів;
- розподіл полісів за статусами;
- аналітика по менеджерах;
- таблиця полісів, строк дії яких скоро завершується;
- внутрішні нотатки по страхових випадках.

### Для admin:
- загальні системні показники;
- контроль проблемних записів;
- журнал останніх дій користувачів;
- заявки на скидання паролів;
- доступ до адміністративних і довідкових розділів системи.

---

## Бізнес-логіка статусів

### Статус полісу

Статус полісу визначається автоматично за правилами:

- якщо є оплачений платіж і строк дії полісу ще не завершено -> **active**
- якщо є оплачений платіж і строк дії полісу завершено -> **completed**
- якщо оплати немає і строк оплати минув -> **canceled**
- в інших випадках -> **draft**

### Статус оплати

Оплата залежить від методу та стану проведення:

- `draft` - чернетка платежу без вибраного способу оплати;
- `scheduled` - запланований банківський переказ;
- `paid` - оплачено;
- `overdue` - строк оплати минув;
- `canceled` - платіж скасовано;
- `refunded` - платіж повернено.

Статуси оплат додатково обмежуються методом оплати:

- для `no_method` доступні `draft`, `overdue`, `canceled`;
- для `cash` і `card` доступні `paid`, `canceled`, `refunded`;
- для `transfer` доступні `scheduled`, `paid`, `overdue`, `canceled`, `refunded`.

---

## Особливості demo-логіки

Проєкт розроблявся як навчальний без інтеграції з реальною платіжною системою.

Тому для демонстрації життєвого циклу оплат і змін статусів у системі використовується окрема demo-логіка:

- щоденна команда `policies:daily-sweep`;
- автоматичне переведення частини актуальних запланованих банківських переказів у статус `paid` за допомогою параметра `--rate`;
- автоматичне позначення прострочених оплат;
- створення службових сповіщень про прострочені платежі.

Цей механізм використовується саме для демонстрації бізнес-логіки в умовах відсутності інтеграції з зовнішнім платіжним провайдером.

---

## Поточний функціонал

На даний момент у проєкті реалізовано:

- CRUD для основних сутностей;
- рольове розмежування доступу;
- менеджер працює лише зі своїми даними;
- створення клієнтів і вхідних заявок;
- конвертація заявки в клієнта;
- створення полісів;
- облік оплат;
- автоматичний перерахунок статусу полісу;
- реєстрація страхових випадків;
- нотатки до страхових випадків;
- журнал дій користувачів;
- дашборди та віджети для різних ролей;
- базові тести на доступи, логіку статусів, дашборд і ключові сервіси.

---

## Приклад бізнес-процесу

1. Менеджер отримує вхідну заявку або створює клієнта вручну.
2. Заявка призначається відповідальному менеджеру.
3. На основі заявки формується картка клієнта.
4. Менеджер обирає страхову пропозицію та створює поліс.
5. Система автоматично встановлює строк оплати.
6. Менеджер або відповідальний співробітник фіксує оплату.
7. Система автоматично оновлює статус полісу.
8. У разі страхового випадку створюється заява та ведуться нотатки по кейсу.
9. Supervisor контролює роботу менеджерів і загальну бізнес-картину по відділу.
10. Admin контролює технічний стан системи, користувачів, довідники та проблемні записи.

---

## Технологічний стек

- **PHP 8.2+**
- **Laravel 12**
- **Filament 4**
- **Tailwind CSS 4**
- **Vite**
- **MySQL**
- **SQLite** (лише для тестів)
- **PHPUnit**

---

## Встановлення та запуск

### 1. Клонування репозиторію

```bash
git clone https://github.com/meowam/finance-crm.git
cd finance-crm
```

### 2. Встановлення PHP-залежностей

```bash
composer install
```

### 3. Встановлення frontend-залежностей

```bash
npm install
```

### 4. Підготовка середовища

```bash
cp .env.example .env
php artisan key:generate
```

### 5. Налаштування бази даних

Для локального запуску проєкту та роботи з demo-даними використовується **MySQL**.

У `.env` потрібно вказати:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=finance
DB_USERNAME=root
DB_PASSWORD=
```

Після цього виконати міграції:

```bash
php artisan migrate
```

> **Примітка:** SQLite у проєкті використовується лише для автоматичних тестів через `phpunit.xml`.

### 6. Наповнення тестовими даними

```bash
php artisan db:seed
```

### 7. Запуск проєкту

Базовий локальний запуск:

```bash
php artisan serve
```

За потреби frontend-ресурси можна окремо запустити через Vite:

```bash
npm run dev
```

Або виконати production-збірку:

```bash
npm run build
```

> У поточному demo-сценарії проєкт зазвичай запускається через `php artisan serve`, а службові команди викликаються окремо вручну.

---

## Demo-команди

Для демонстрації автоматичної логіки оплат і сповіщень можна вручну запускати команду:

```bash
php artisan policies:daily-sweep
```

Або з власним коефіцієнтом переходу запланованих переказів у статус `paid`:

```bash
php artisan policies:daily-sweep --rate=0.35
```

Це demo-механізм для моделювання змін статусів оплат у pet project.

---

## Вхід до системи

Панель доступна за адресою:

```text
/admin
```

Після виконання сидерів доступні тестові користувачі:

### Admin

- **email:** `admin@insurance.local`
- **password:** `admin123`

### Supervisor

- **email:** `supervisor1@insurance.local`
- **password:** `supervisor123`

### Manager

- **email:** `manager1@insurance.local`
- **password:** `manager123`

---

## Тести

Запуск усіх тестів:

```bash
php artisan test
```

Або через PHPUnit:

```bash
vendor/bin/phpunit
```

---

## Структура основних модулів

- **Clients** - клієнти
- **LeadRequests** - вхідні заявки
- **Policies** - поліси
- **PolicyPayments** - оплати полісів
- **Claims** - страхові випадки
- **ClaimNotes** - нотатки до заяв
- **Users** - користувачі
- **ActivityLogs** - журнал дій користувачів
- **InsuranceCategories / InsuranceCompanies / InsuranceProducts / InsuranceOffers** - страхові довідники

---


## Ліцензія

Проєкт розповсюджується на умовах ліцензії **MIT**.